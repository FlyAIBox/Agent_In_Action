# ============================================================================
# AI旅行规划智能体 - 环境变量配置文件
# ============================================================================
#
# 这个文件定义了系统运行所需的所有环境变量和API密钥。
# 环境变量是一种安全的方式来存储敏感信息（如API密钥），
# 避免将这些信息直接写在代码中。
#
# 适用于大模型技术初级用户：
# - 环境变量是现代软件开发的标准实践
# - 它们允许在不修改代码的情况下配置应用程序
# - 这种方式提高了安全性和部署的灵活性
# ============================================================================

# ----------------------------------------------------------------------------
# 🚀 核心API配置 (AI旅行规划智能体必需)
# ----------------------------------------------------------------------------

# OpenAI 兼容 API 密钥 (必需 - AI旅行规划智能体的核心)
# 功能说明：
# - 支持官方 OpenAI 及国内常见的 OpenAI 兼容网关（如 DeepSeek、通义千问等）
# - 用于驱动所有 AI 智能体的推理和决策
# - 支持通过自建代理实现国内可访问的模型调用
# - 获取方式：根据所选服务商在控制台创建密钥
OPENAI_API_KEY=your_openai_style_api_key_here

# OpenAI 兼容接口基础地址 (可选)
# 功能说明：
# - 自定义 OpenAI 风格接口的 Base URL
# - 例如使用 DeepSeek 可填写：https://api.deepseek.com/v1
# - 若使用官方 OpenAI，可保留默认值 https://api.openai.com/v1
OPENAI_BASE_URL=https://api.deepseek.com/v1

# OpenAI 兼容模型名称 (可选)
# 功能说明：
# - 指定默认调用的模型名称
# - 可根据服务商提供的模型（如 deepseek-chat、qwen-max、gpt-4o-mini 等）进行调整
OPENAI_MODEL=deepseek-chat

# ----------------------------------------------------------------------------
# 🔧 传统系统API配置 (可选 - 用于兼容原始系统)
# ----------------------------------------------------------------------------

# 和风天气 (QWeather) API配置 (可选)
# 功能说明：
# - 提供国内稳定可访问的实时天气、灾害预警、多日预报
# - 本项目默认使用和风天气替换 OpenWeather
# - 开发者平台：https://dev.qweather.com/
QWEATHER_API_KEY=your_qweather_api_key_here
QWEATHER_API_BASE=your_api_host

# 高德地图 (AMap) API配置 (可选)
# 功能说明：
# - 提供地点检索、POI 信息、交通与地图服务
# - 用于景点、餐厅、酒店等本地化搜索
# - 开发者平台：https://lbs.amap.com/
AMAP_API_KEY=your_amap_api_key_here
AMAP_BASE_URL=https://restapi.amap.com

# 汇率查询服务配置 (可选)
# ExchangeRate-API
# 现在要求注册并获取 API Key，但其免费套餐通常很慷慨，而且在不同地区（包括亚洲）都有良好的声誉和稳定性。
# API 地址格式（获取最新汇率）：
# https://v6.exchangerate-api.com/v6/YOUR-API-KEY/latest/CNY
# 你需要到其官网注册一个免费账户来获取 YOUR-API-KEY。
# 特点：
# 通常提供 160+ 种货币的汇率。
# 免费套餐通常提供足够的请求量（例如，每月几万次），适合个人项目和低流量应用。
# 它是一个专业的 API 服务，比一些完全的“开源”项目更稳定。
EXCHANGE_RATE_API_BASE=https://v6.exchangerate-api.com/v6/YOUR-API-KEY/latest/CNY

# ============================================================================
# 📋 配置说明和使用指南
# ============================================================================
#
# 🎯 快速开始 (仅需1步)：
# 1. 将此文件复制为 .env
# 2. 将 OPENAI_API_KEY 替换为您的实际API密钥
# 3. 运行系统：python langgraph_main.py
#
# 🔍 详细配置说明：
#
# 1. 📁 文件复制：
#    cp env.example .env
#    (将示例文件复制为实际配置文件)
#
# 2. 🔑 获取 OpenAI 兼容密钥：
#    - 根据所选模型服务商（如 DeepSeek、通义千问、零一万物等）申请 API Key
#    - 若使用自建代理，请在代理控制台创建访问密钥
#    - 将创建的密钥填写到 OPENAI_API_KEY
#
# 3. 🚀 系统选择：
#    - 新用户推荐：AI旅行规划智能体 (仅需 OPENAI_API_KEY)
#    - 完整体验：配置所有API密钥
#    - 测试模式：使用模拟数据 (无需API密钥)
#
# 4. 🔒 安全注意事项：
#    - 永远不要将 .env 文件提交到版本控制系统
#    - 定期更换API密钥
#    - 不要在公共场所分享API密钥
#    - 监控API使用量和费用
#
# 5. 🛠️ 故障排除：
#    - 确保 .env 文件在项目根目录
#    - 检查API密钥格式是否正确
#    - 验证API密钥是否有效且未过期
#    - 确认API配额是否充足
#
# ============================================================================
# 🌟 系统功能对比
# ============================================================================
#
# AI旅行规划智能体 (推荐)：
# ✅ 仅需 OPENAI_API_KEY
# ✅ 实时搜索集成 (DuckDuckGo)
# ✅ 多智能体协作
# ✅ 高级AI推理能力
# ✅ 免费搜索功能
#
# 传统系统：
# 🔧 需要多个API密钥
# 🔧 静态数据源
# 🔧 单一智能体
# 🔧 基础功能
# 🔧 API费用较高
#
# ============================================================================
