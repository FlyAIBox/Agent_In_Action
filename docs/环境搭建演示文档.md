# 环境搭建演示文档

> 本文档提供 Agent_In_Action 项目完整的环境搭建指南，涵盖从系统安装到开发环境配置的全流程。

---

## 📋 目录

- [1. 系统要求](#1-系统要求)
- [2. 虚拟机环境搭建（可选）](#2-虚拟机环境搭建可选)
- [3. Ubuntu 22.04.4 系统安装](#3-ubuntu-22044-系统安装)
- [4. Python 和 Conda 环境安装](#4-python-和-conda-环境安装)
- [5. v2raya 科学上网工具（可选）](#5-v2raya-科学上网工具可选)
- [6. JupyterLab 安装与配置](#6-jupyterlab-安装与配置)
- [7. 项目代码下载](#7-项目代码下载)
- [8. 环境变量配置](#8-环境变量配置)
- [9. JupyterLab 服务管理](#9-jupyterlab-服务管理)
- [10. 验证环境](#10-验证环境)

---

## 1. 系统要求

### 基础配置

| 组件 | 推荐配置 |
|------|----------|
| **操作系统** | Ubuntu 22.04 LTS |
| **Python** | 3.10.x |
| **CPU** | 4 核心 |
| **内存** | 8 GB+ |
| **存储** | 100 GB+ |
| **GPU** | 非必需（使用 API 调用） |

### 高级配置（模型微调场景）

| 组件 | 要求 | 说明 |
|------|------|------|
| **GPU** | NVIDIA GPU | 支持 CUDA 12.8+ |
| **显存** | 80GB+ | LoRA 微调最低要求 |
| **内存** | 32GB+ | 推荐配置 |

---

## 2. 虚拟机环境搭建（可选）

如果你使用 Windows 或 macOS 系统，可以通过虚拟机安装 Ubuntu。

### 2.1 VMware Workstation Pro（推荐）

**参考文档**：[VMware Workstation Pro 17 安装教程](./VMware%20Workstation%20Pro%2017%20安装教程.md)

**主要步骤**：
1. 下载并安装 VMware Workstation Pro 17
2. 创建新的虚拟机
3. 配置虚拟机硬件（CPU、内存、硬盘）
4. 安装 Ubuntu 22.04.4 系统镜像

### 2.2 VirtualBox（免费替代方案）

**参考文档**：[VirtualBox 安装 Ubuntu22.04 与系统开发环境配置](./VirtualBox%20安装%20Ubuntu22.04%20与系统开发环境配置.md)

**主要步骤**：
1. 下载并安装 VirtualBox 7.0
2. 下载 Ubuntu 22.04.4 系统镜像
3. 创建虚拟机并配置网络（NAT 模式 + 端口转发）
4. 安装增强功能

---

## 3. Ubuntu 22.04.4 系统安装

### 3.1 获取系统镜像

从清华大学开源软件镜像站下载 Ubuntu 22.04.4：

```bash
# 镜像站地址
https://mirrors.tuna.tsinghua.edu.cn/
# 选择：Ubuntu → 22.04.5 (amd64, Desktop LiveDVD)
```

### 3.2 系统安装要点

1. **语言选择**：建议安装过程中使用 English，安装后再切换中文
2. **用户创建**：记住用户名和密码，后续配置会用到
3. **分区方案**：虚拟机可使用默认分区，物理机建议自定义分区
4. **安装后设置**：
   - 更新系统软件源
   - 安装必要的开发工具
   - 配置系统语言和时区

### 3.3 系统初始化

```bash
# 更新系统包
sudo apt update && sudo apt upgrade -y

# 安装必要的系统依赖
sudo apt install -y wget curl git build-essential vim net-tools
```

---

## 4. Python 和 Conda 环境安装

### 4.1 安装 Miniconda

```bash
# 下载 Miniconda 安装包
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh

# 安装 Miniconda（默认安装到 $HOME/miniconda3）
bash Miniconda3-latest-Linux-x86_64.sh -b -p $HOME/miniconda3

# 初始化 conda
$HOME/miniconda3/bin/conda init bash

# 重新加载 shell 配置
source ~/.bashrc

# 验证 conda 安装
conda --version
```

### 4.2 创建项目专用环境

```bash
# 创建 conda 环境（Python 3.10.18）
conda create -n agent101 python=3.10.18 -y

# 激活环境
conda activate agent101

# 验证 Python 版本
python --version
```

### 4.3 配置 Conda 镜像源（可选）

为了加速包下载，可以配置清华镜像源：

```bash
# 配置 conda 镜像
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free
conda config --set show_channel_urls yes

# 配置 pip 镜像
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 5. v2raya 科学上网工具（可选）

> ⚠️ 注意：科学上网工具仅在需要访问国外资源时使用，请遵守相关法律法规。

### 5.1 安装 v2raya

```bash
# 添加公钥
wget -qO - https://apt.v2raya.org/key/public-key.asc | sudo tee /etc/apt/keyrings/v2raya.asc

# 添加软件源
echo "deb [signed-by=/etc/apt/keyrings/v2raya.asc] https://apt.v2raya.org/ v2raya main" | sudo tee /etc/apt/sources.list.d/v2raya.list

# 更新软件源
sudo apt update

# 安装 v2raya
sudo apt install -y v2raya v2ray
```

### 5.2 启动 v2raya 服务

```bash
# 启动并设置开机自启
sudo systemctl enable v2raya.service
sudo systemctl start v2raya.service

# 查看服务状态
sudo systemctl status v2raya.service
```

### 5.3 配置 v2raya

1. 在浏览器中访问：`http://localhost:2017`
2. 创建管理员账号
3. 导入订阅链接或手动添加节点
4. 选择节点并启动代理

---

## 6. JupyterLab 安装与配置

> 详细配置请参考：[Jupyter Lab 安装、配置和常见操作](./Jupyter%20Lab%20安装、配置和常见操作.md)

### 6.1 安装 JupyterLab

```bash
# 激活 conda 环境
conda activate agent101

# 使用 conda 安装 JupyterLab
conda install -c conda-forge jupyterlab -y

# 验证安装
jupyter lab --version
```

### 6.2 生成配置文件

```bash
# 生成 JupyterLab 配置文件
jupyter lab --generate-config

# 输出信息：
# Writing default config to: /root/.jupyter/jupyter_lab_config.py
```

### 6.3 修改配置文件

编辑配置文件 `/root/.jupyter/jupyter_lab_config.py`：

```bash
# 编辑配置文件
vim /root/.jupyter/jupyter_lab_config.py
```

添加或修改以下配置：

```python
# 允许 root 用户启动
c.ServerApp.allow_root = True

# 设置工作目录
c.ServerApp.root_dir = '/Agent101/code'

# 修改默认端口
c.ServerApp.port = 8000

# 设置可访问的 IP（0.0.0.0 表示不限制）
c.ServerApp.ip = '0.0.0.0'

# 设置访问令牌（密码）
c.ServerApp.token = 'your_token_here'
```

### 6.4 安装 ipykernel（重要）

```bash
# 在 agent101 环境中安装 ipykernel
conda activate agent101
pip install ipykernel

# 将环境注册为 Jupyter 内核
python -m ipykernel install --user --name=agent101 --display-name="Python (agent101)"

# 验证内核安装
jupyter kernelspec list
```

---

## 7. 项目代码下载

### 7.1 创建代码目录

```bash
# 创建代码存放目录
sudo mkdir -p /Agent101/code

# 修改目录所有者（替换 your_username 为实际用户名）
sudo chown -R $USER:$USER /Agent101/code

# 进入代码目录
cd /Agent101/code
```

### 7.2 克隆项目代码

```bash
# 克隆项目仓库
git clone https://github.com/FlyAIBox/Agent_In_Action.git

# 进入项目目录
cd Agent_In_Action

# 查看项目结构
ls -la
```

### 7.3 按项目安装依赖

```bash
# 激活 conda 环境
conda activate agent101

# 项目1-2：智能体基础与 MCP
cd 01-agent-tool-mcp/mcp-demo
pip install -r requirements.txt

# 项目3：深度研究助手
cd ../../02-agent-multi-role/deepresearch/deployment
pip install -r requirements.txt

# 项目4：旅行规划系统
cd ../../../03-agent-build-docker-deploy
pip install -r backend/requirements.txt
pip install -r frontend/requirements.txt

# 项目5：评估监控
pip install langfuse langchain langgraph

# 返回项目根目录
cd /Agent101/code/Agent_In_Action
```

---

## 8. 环境变量配置

### 8.1 配置方式说明

环境变量可以通过以下两种方式配置：

1. **全局配置**：添加到 `~/.bashrc` 文件（推荐）
2. **项目配置**：在项目目录创建 `.env` 文件

### 8.2 配置环境变量到 ~/.bashrc

编辑 `~/.bashrc` 文件：

```bash
vim ~/.bashrc
```

在文件末尾添加以下内容：

```bash
# ==========================================================
# API and Environment Configuration
# ==========================================================

# --- OpenAI Configuration ---
export OPENAI_BASE_URL="your_openai_base_url"
export OPENAI_API_KEY="your_openai_api_key"
export MODEL_NAME="gpt-4o"

# --- DeepSeek Configuration ---
export DEEPSEEK_BASE_URL="https://api.deepseek.com"
export DEEPSEEK_API_KEY="your_deepseek_api_key"

# --- Langfuse Configuration ---
export LANGFUSE_HOST="https://cloud.langfuse.com"
export LANGFUSE_PUBLIC_KEY="your_langfuse_public_key"
export LANGFUSE_SECRET_KEY="your_langfuse_secret_key"

# --- Search Tool Configuration ---
export SERPAPI_API_KEY="your_serpapi_api_key"
export TAVILY_API_KEY="your_tavily_api_key"

# --- LangSmith Configuration ---
export LANGSMITH_API_KEY="your_langsmith_api_key"
export LANGSMITH_PROJECT="aiagent101"

# --- Qweather API Configuration ---
# 和风天气 API 配置（参考 https://dev.qweather.com/）
export QWEATHER_API_BASE="your_qweather_api_base"
export QWEATHER_API_KEY="your_qweather_api_key"
```

使配置生效：

```bash
source ~/.bashrc
```

### 8.3 API 密钥获取指南

| API 服务 | 获取地址 | 用途 | 费用 |
|---------|---------|------|------|
| **OpenAI** | [platform.openai.com](https://platform.openai.com) | 基础模型调用 | 按量付费 |
| **DeepSeek** | [platform.deepseek.com](https://platform.deepseek.com) | 中文优化模型 | 价格实惠 |
| **和风天气** | [dev.qweather.com](https://dev.qweather.com) | 天气查询 | 免费额度 |
| **Tavily** | [tavily.com](https://tavily.com) | 网络搜索 | 免费额度 |
| **SerpAPI** | [serpapi.com](https://serpapi.com) | Google 搜索 | 免费额度 |
| **Langfuse** | [cloud.langfuse.com](https://cloud.langfuse.com) | 监控评估 | 免费版可用 |
| **LangSmith** | [smith.langchain.com](https://smith.langchain.com) | 调试追踪 | 免费额度 |

---

## 9. JupyterLab 服务管理

### 9.1 创建 Conda 启动脚本

创建启动脚本目录和文件：

```bash
# 创建脚本目录
sudo mkdir -p /Agent101/app/jupyter

# 创建启动脚本
sudo vim /Agent101/app/jupyter/start_jupyter.sh
```

添加以下内容到脚本文件：

```bash
#!/bin/bash

# ==========================================================
# API and Environment Configuration
# 注意：这里需要配置实际的 API Key，或者通过其他方式加载
# ==========================================================

# --- OpenAI Configuration ---
export OPENAI_BASE_URL="your_openai_base_url"
export OPENAI_API_KEY="your_openai_api_key"
export MODEL_NAME="gpt-4o"

# --- DeepSeek Configuration ---
export DEEPSEEK_BASE_URL="https://api.deepseek.com"
export DEEPSEEK_API_KEY="your_deepseek_api_key"

# --- Langfuse Configuration ---
export LANGFUSE_HOST="https://cloud.langfuse.com"
export LANGFUSE_PUBLIC_KEY="your_langfuse_public_key"
export LANGFUSE_SECRET_KEY="your_langfuse_secret_key"

# --- Search Tool Configuration ---
export SERPAPI_API_KEY="your_serpapi_api_key"
export TAVILY_API_KEY="your_tavily_api_key"

# --- LangSmith Configuration ---
export LANGSMITH_API_KEY="your_langsmith_api_key"
export LANGSMITH_PROJECT="aiagent101"

# --- Qweather API Configuration ---
export QWEATHER_API_BASE="your_qweather_api_base"
export QWEATHER_API_KEY="your_qweather_api_key"

# ==========================================================
# Conda 初始化逻辑
# ==========================================================
__conda_setup="$('/root/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/root/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/root/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/root/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup

# 激活 conda 环境
conda activate agent101

# 运行 JupyterLab（使用 exec 确保进程正确跟踪）
exec /usr/local/bin/jupyter-lab --config=/root/.jupyter/jupyter_lab_config.py --no-browser
```

设置脚本执行权限：

```bash
sudo chmod +x /Agent101/app/jupyter/start_jupyter.sh
```

### 9.2 创建 systemd 服务

创建服务配置文件：

```bash
sudo vim /etc/systemd/system/jupyter.service
```

添加以下内容：

```ini
[Unit]
Description=Jupyterlab
After=network.target

[Service]
Type=simple
ExecStart=/Agent101/app/jupyter/start_jupyter.sh
User=root
Group=root
WorkingDirectory=/Agent101/code
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 9.3 启动和管理 JupyterLab 服务

```bash
# 重载 systemd 配置
sudo systemctl daemon-reload

# 启用开机自启
sudo systemctl enable jupyter.service

# 启动服务
sudo systemctl start jupyter.service

# 查看服务状态
sudo systemctl status jupyter.service

# 查看服务日志
sudo journalctl -u jupyter.service -f

# 停止服务
sudo systemctl stop jupyter.service

# 重启服务
sudo systemctl restart jupyter.service
```

### 9.4 访问 JupyterLab

在浏览器中访问：

```
http://localhost:8000
# 或
http://your_server_ip:8000
```

输入配置的 token 进行登录。

---

## 10. 验证环境

### 10.1 验证 Python 和 Conda

```bash
# 检查 conda 版本
conda --version

# 检查 Python 版本
python --version

# 检查已安装的环境
conda env list

# 检查当前环境的包
pip list
```

### 10.2 验证 JupyterLab

```bash
# 检查 JupyterLab 版本
jupyter lab --version

# 检查已注册的内核
jupyter kernelspec list
```

### 10.3 验证环境变量

```bash
# 检查环境变量是否正确配置
echo $OPENAI_API_KEY
echo $DEEPSEEK_API_KEY
echo $LANGFUSE_PUBLIC_KEY
# ... 检查其他环境变量
```

### 10.4 在 JupyterLab 中测试

打开 JupyterLab，创建一个新的 Notebook，运行以下代码：

```python
# 测试环境变量
import os

print("OpenAI API Key:", os.getenv("OPENAI_API_KEY"))
print("DeepSeek API Key:", os.getenv("DEEPSEEK_API_KEY"))

# 测试 Python 版本
import sys
print("Python Version:", sys.version)

# 测试安装的包
import langchain
import langgraph
print("LangChain Version:", langchain.__version__)
```

---

## 📚 相关文档

- [README.md](../README.md) - 项目总览
- [Jupyter Lab 安装、配置和常见操作](./Jupyter%20Lab%20安装、配置和常见操作.md)
- [VirtualBox 安装 Ubuntu22.04 与系统开发环境配置](./VirtualBox%20安装%20Ubuntu22.04%20与系统开发环境配置.md)
- [VMware Workstation Pro 17 安装教程](./VMware%20Workstation%20Pro%2017%20安装教程.md)

---

## 🔧 故障排除

### 问题 1：Conda 命令不可用

**解决方案**：
```bash
# 重新初始化 conda
$HOME/miniconda3/bin/conda init bash
source ~/.bashrc
```

### 问题 2：JupyterLab 中 conda 环境不可用

**解决方案**：
```bash
# 确保 ipykernel 已安装
conda activate agent101
pip install ipykernel
python -m ipykernel install --user --name=agent101 --display-name="Python (agent101)"
```

### 问题 3：systemd 服务启动失败

**解决方案**：
```bash
# 查看详细日志
sudo journalctl -u jupyter.service -n 50

# 检查脚本权限
ls -l /Agent101/app/jupyter/start_jupyter.sh

# 检查工作目录是否存在
ls -ld /Agent101/code
```

### 问题 4：端口被占用

**解决方案**：
```bash
# 查看端口占用情况
sudo netstat -tulpn | grep 8000

# 修改配置文件中的端口号
vim /root/.jupyter/jupyter_lab_config.py
# 修改 c.ServerApp.port = 8001
```

---

## ⚠️ 注意事项

1. **安全性**：
   - 不要在公开的配置文件中暴露 API Key
   - 生产环境建议使用更安全的密钥管理方案
   - JupyterLab 访问令牌要设置复杂的密码

2. **性能优化**：
   - 根据实际需求调整虚拟机资源分配
   - 使用 SSD 存储提升 I/O 性能
   - 定期清理 conda 缓存：`conda clean -a`

3. **备份建议**：
   - 定期备份环境配置和项目代码
   - 虚拟机建议创建快照
   - 重要数据使用版本控制

4. **更新维护**：
   - 定期更新系统包：`sudo apt update && sudo apt upgrade`
   - 定期更新 conda：`conda update conda`
   - 定期更新项目依赖：`pip list --outdated`

---

## 📞 获取帮助

如果在环境搭建过程中遇到问题，可以通过以下方式获取帮助：

- 🐛 **Bug 报告**：[GitHub Issues](https://github.com/FlyAIBox/Agent_In_Action/issues)
- 📧 **邮件联系**：fly910905@sina.com
- 🔗 **微信公众号**：萤火AI百宝箱

---

**文档版本**：v1.0  
**更新日期**：2025-11-14  
**维护者**：FlyAIBox Team

